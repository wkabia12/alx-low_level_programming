#include <stdio.h>
/**
 * main - Returns all possible combos of 2 digits
 * Return: 0
 */
int main(void)
{
	int i = '0'
	
	for (i = '0', i <=9 '0', i++)
	{
		putchar(i);
		for (k = i+1; k <=9; k++)
		{
			putchar(k);

		}
	}
}
