#include "main.h"

/**
 * mul - multiples 2 numbers
 * @a: argument to be multipled
 * @b: argument to be multipled
 * Return: 1 if uppercase, 0 otherwise
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}

