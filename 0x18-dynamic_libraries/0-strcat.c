#include "main.h"

/**
 * _strcat - concatenates 2 strings
 * @dest: string1
 * @src: string2
 * Return: pointer to dest
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
