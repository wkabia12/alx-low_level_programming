#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: argument to be checked
 * Return: 1 if uppercase, 0 otherwise
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
