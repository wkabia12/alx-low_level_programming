#include "main.h"

/**
 * _strcmp - compares 2 strings
 * @s1: string1
 * @s2: string2
 * Return: 0 if matching, else ASCII diff 1st non-matching xters
 */
int _strcmp(char *s1, char *s2)
{
	int match = strcmp(s1, s2);

	return (match);
}
